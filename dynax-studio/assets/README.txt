DynaX Studio assets
===================

Drop the DynaX logo here as `logo.png` (recommended ~256x256 PNG with
transparency). The launcher window currently uses a styled text title; once a
logo file is present it can be shown above the title in main_window.py.

No binary asset is committed in Sprint 0 to keep the repo light.
